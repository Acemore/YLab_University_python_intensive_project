services:
  api-tests:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - LOCAL_URL=http://api:8000
    command: [ "/bin/bash", "-c", "poetry run pytest menu_app_tests/*"]
